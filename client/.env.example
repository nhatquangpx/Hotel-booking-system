# Sao chép file này thành .env và điền giá trị thật (không commit .env lên git)
# cp .env.example .env

# Base URL API (có /api ở cuối). Axios + cookie withCredentials dùng biến này.
VITE_API_URL=http://localhost:8001/api
