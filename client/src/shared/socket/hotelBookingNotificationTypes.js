/** Loại thông báo khách sạn liên quan đặt phòng (socket `new_notification`). */
export const HOTEL_BOOKING_NOTIFICATION_TYPES = new Set([
  'new_booking',
  'booking_cancelled',
  'checkin_today',
  'checkout_today',
]);
